<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hasil Deteksi</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f9f9f9;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .result-container {
        background-color: #fff;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      h1 {
        font-size: 2rem;
        color: #333;
        margin-bottom: 10px;
      }

      .detected-number {
        font-size: 3rem;
        color: #007bff;
        font-weight: bold;
        margin-top: 0;
      }

      .scan-button {
        padding: 20px 40px;
        font-size: 1.5rem;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        transition: background-color 0.3s ease;
      }

      .scan-button:hover {
        background-color: #0056b3;
      }

      .scan-button:focus {
        outline: 3px solid #ffcc00;
      }
    </style>
  </head>
  <body>
    <div class="result-container">
      <h1>Hasil Deteksi:</h1>
      <p class="detected-number"><%= number %></p>

      <a class="scan-button" href="/scan">Scan lagi</a>
    </div>
  </body>
</html>
